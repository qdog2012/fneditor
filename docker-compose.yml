services:
  fneditor:
    build: .
    container_name: fneditor
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - FNEDITOR_ROOT=/data
    volumes:
      - /vol1/1000:/data
